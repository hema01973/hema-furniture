import type { Metadata } from 'next';
import AccountPage from '@/components/account/AccountPage';
export const metadata: Metadata = { title: 'My Account — Hema Furniture' };
export default function Page() { return <AccountPage />; }
